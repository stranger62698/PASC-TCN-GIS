# 澜迹 InSAR V2 项目审计（Phase 0）

审计日期：2026-08-21  
审计范围：现有路由、页面组件、地图状态、数据结构、CSV 导入、Statistics、登录、私有数据、AI 接口、Demo 数据与部署结构。  
本阶段约束：只审计，不修改 UI、不新增路由、不更换技术栈。

## 1. 审计结论

现有项目已经不是一个简单地图 Demo：公开示例、CSV 字段识别、WebGIS 图层控制、点位时序、多点对比、区域框选、数据质检、登录和私有文件分块存储都已有可复用实现。

V2 的主要工作不应是继续堆功能，而应解决三个产品问题：

1. 首页、Map、Statistics 与 Dataset 之间缺少一条连续的用户任务链。
2. Map 的能力集中在一个大型组件中，功能存在但入口和分析状态不统一。
3. AI 解读、分析上下文、真实 Statistics 和产品埋点尚未建立；仓库中还并存多套历史运行方案，增加维护风险。

建议严格按 V2 的 Phase 1—9 继续，优先完成“首页进入 Demo → 发现异常 → 点位/区域分析 → AI 解读 → 输出”，暂不横向扩展新业务。

## 2. 当前运行结构

### 2.1 页面路由

| 路由 | 当前组件 | 当前状态 | V2 处理方向 |
| --- | --- | --- | --- |
| `/` | `HomePage` | 可用，但内容偏官网型且信息较多 | Phase 1 收束为理解产品 + 进入 Demo |
| `/map` | `MapWorkspace` + `WebGisMap` | 核心功能可用，状态集中、入口较多 | Phase 2—5 改为统一分析工作台 |
| `/statistics` | `ContentPage` | 页面存在，但指标和图表为固定展示数据 | Phase 7 接入 Map 分析上下文 |
| `/datasets` | `DatasetPage` | 登录后可分块上传、列出、重命名、重传、质检、删除 | Phase 6 补齐确认式导入流程 |
| `/showcase` | `ShowcasePage` | 三类案例可切换 | Phase 8 改成统一业务叙事 |
| `/showcase/:id` | `CaseDetailPage` | 城市、滑坡、公路详情可访问 | Phase 8 去模板化、明确演示数据边界 |
| `/platform` | `ContentPage` | 能力与架构说明存在 | Phase 8 只保留四项核心能力 |
| `/solutions` | `ContentPage` | 与首页、Platform 有重复 | Phase 8 从一级导航弱化 |
| `/about` | `ContentPage` | 作品说明存在 | Phase 8 强化个人产品实践 |
| `/login` | `AuthPage` | 注册/登录可用，忘记密码未完成 | Phase 8 简化为“先体验，后登录” |

生产构建当前由 Vite 输出静态 SPA，`static-src/main.tsx` 维护一份前端路由映射；仓库同时保留 Next/Vinext 页面文件。该双入口可以暂时维持现有发布，但每次增改路由都需要同步检查两处。

### 2.2 数据与服务

- 当前公开示例：`public/data/haikou-insar.csv`，约 8.0 MB、4,073 行监测点、214 列，其中约 210 期时序字段。
- 当前前端统一点结构：`InsarPoint`，包含编号、经纬度、长期速率、累计形变、相干性、缺测率、模式、日期和完整时序。
- 当前生产认证：Vercel Functions `api/auth.ts` + HttpOnly 会话 Cookie。
- 当前私有存储：Vercel Blob `api/private-datasets.ts`，按 `users/{userId}/...` 隔离元数据和 CSV 分块。
- 仓库还保留一套 Cloudflare/Sites D1 + R2 + ChatGPT 身份接口，当前 Vercel 静态发布主流程没有调用它。
- 当前没有可调用的 LLM/AI 分析接口，也没有统一 `analysisContext` 或 `trackEvent`。

## 3. 现有能力

### 3.1 首页与内容页

- 已有统一品牌、固定导航、响应式导航、悬停下拉、案例切换和滚动入场效果。
- 首页已有地图主视觉、指标条、场景案例、平台演示、流程和 CTA。
- 城市、滑坡、公路三类案例已有图片和详情页。
- Platform、Solutions、About、Statistics 均有可访问路由，不存在空链接。

### 3.2 Map / GIS

- OSM、Esri World Imagery、Esri Light Gray、天地图矢量/影像、自定义 XYZ/WMTS。
- 天地图 Key 和自定义图源保存在当前浏览器；底图版权信息已显示。
- 底图单选、业务图层复选，勾选状态真实控制图层显隐。
- 左右侧栏可折叠、可拖动调整宽度。
- 鼠标移动显示 WGS84 经纬度。
- CSV 导入后自动计算 bbox 并 `fitBounds` 到实际数据范围。
- 点图层、重点形变点、低相干/高缺测图层、数据外包范围。
- 平均速率、当前累计形变、阶段速率、模式、相干性、缺测率切换渲染。
- 数值范围、间距和色带可调，支持导入 QGIS QML/XML 色带。
- 时间轴可切换当前观测期，并按该期累计形变重新着色。
- 单点选择、多点选择（最多 30 条曲线）、矩形区域统计、选中高亮、清除选择。
- 点位详情、线性拟合、带横纵轴的时序曲线、单点 CSV 导出。
- 地图截图和浏览器打印入口。

### 3.3 CSV 与质量检查

- 自动识别 longitude/latitude/lon/lat/经纬度、velocity/rate、label/mode/pattern、coherence/accuracy、研究区名称和日期列。
- 缺少点位编号时按有效行顺序生成编号；存在编号列时保留原值。
- 模式字段缺失时，可尝试识别 0—5 的低基数分类列；否则标记“未分类”。
- 过滤非法经纬度、(0,0)、时序不足和非法速率，并保留最多 20 条错误样例。
- 输出无效行、缺测率、低相干点、速率异常值、模式分布、日期列和 bbox。
- 数据集标题优先读取研究区字段，否则使用文件名，不再强制写“海口”。

### 3.4 账户与数据集

- 邮箱 + 密码注册、用户名展示、登录、退出、会话检查。
- 密码使用 scrypt 加盐哈希；会话使用签名 HttpOnly Cookie。
- 私有文件路径按服务端用户 ID 隔离。
- 数据集列表、重命名、删除、重新导入/版本号、上传进度、质检报告入口。
- 单文件上限 2 GB、单账户 5 GB 的服务端限制。
- 公共示例和本地 CSV 分析无需登录；保存私有数据集需要登录，符合“先体验，后登录”的基础边界。

## 4. 重复能力

### 4.1 两套地图组件

- `WebGisMap.tsx` 是当前 `/map` 实际使用的地图。
- `InsarMap.tsx` 是旧实现，未被当前路由引用，仍包含另一套底图、图层、点选和形变模式逻辑。
- 风险：后续修复可能误改旧组件，或让颜色、底图 Key、选择行为继续分叉。

### 4.2 多套后端/身份/存储方案并存

- 当前 Vercel 方案：`api/auth.ts`、`api/private-datasets.ts`、`server/auth.ts`。
- 历史 Sites/Cloudflare 方案：`app/api/datasets/*`、`app/api/uploads/*`、`app/chatgpt-auth.ts`、D1/R2 schema。
- 历史 Netlify 依赖和脚本仍在仓库。
- 风险：接口文档、环境变量和部署判断不一致，开发者难以确认哪套是真实生产链路。

### 4.3 内容表达重复

- 首页、Platform、Solutions、Showcase 都在解释“CSV 接入、时序分析、模式识别、地图发布”。
- 首页、案例页和 Statistics 重复使用 KPI 卡片，但数值口径不一致。
- Login 页面包含较多平台架构营销内容，与“先体验，后登录”的轻量目标不一致。

### 4.4 路由声明重复

- Next/Vinext 文件路由和 `static-src/main.tsx` 的手工路由表同时存在。
- 这是当前静态发布结构的兼容需求，不应立即重写，但后续每个 Phase 都要同步验证两种入口。

## 5. 可以复用

### 5.1 数据基础

- `InsarPoint` 可作为 Phase 2 `AnalysisContext` 的点位基础类型。
- `CsvInspection`、`CsvMapping`、`DatasetParseResult`、`QualityReport` 已覆盖 Phase 6 的主要数据结构。
- `inspectCsv`、`parseMappedCsv`、`stageVelocity`、`renderValue`、`parseQgisRamp` 应继续复用，不重写解析器。
- 当前程序计算的 bbox、阶段速率、缺测率、模式分布可直接作为后续 AI 的结构化依据。

### 5.2 Map 基础

- `WebGisMap` 的底图、图层、`fitBounds`、点选、框选、经纬度、选中高亮和时间着色可保留。
- `TimeSeriesChart`、`CompareChart` 可在 Phase 3 增加 Hover、时间缩放和恢复全时段，不需要换图表库。
- 当前左/右侧栏拖拽、时间轴、QGIS 色带和质量阈值是有价值的专业 GIS 能力，应通过信息架构收束，不应删除。

### 5.3 用户与数据集基础

- Vercel Auth + Blob 用户隔离可继续作为当前发布版本的后端基础。
- `DatasetPage` 的分块上传、列表、重命名、重传、删除和质检弹窗可在 Phase 6 上继续迭代。
- 本地浏览器只保存天地图 Key、图源 URL 等设备偏好，符合浏览器存储边界。

### 5.4 视觉与内容资产

- 统一字体变量、卫星图标、案例图片、案例数据和 Spotlight/Reveal 交互可以复用。
- `PageShell`、`SiteHeader`、`SiteFooter` 可作为 Phase 1 和 Phase 8 的收束入口。

## 6. 需要删除

以下项目不在 Phase 0 直接删除，应在对应 Phase 确认无引用后处理：

1. 未使用的 `InsarMap.tsx` 旧地图实现。
2. 最终确定 Vercel 为唯一生产运行方案后，移除或迁出未使用的 Cloudflare/Sites API、ChatGPT 身份原型和 Netlify 遗留依赖/脚本。
3. Statistics 中与真实选区无关的固定 KPI、固定直方图和固定质量摘要。
4. Solutions、Platform、Home 之间重复的能力卡片和模板化文案。
5. Login 中与登录任务无关的过度架构宣传；保留简洁的隐私和价值说明。
6. “已进入转换队列”等没有真实后台任务支撑的生产文案。
7. README 中已经过期的站点地址、部署方式、Key 配置和身份说明。

## 7. 需要修改

### Phase 1：首页 + Demo

- 全站名称统一为“澜迹 InSAR（LANJIFYW）｜城市地表形变智能分析平台”。
- 首屏只保留用途说明、体验示例数据和上传我的数据两个主动作。
- Demo 链接直接进入 `/map`，无需登录。
- 第二屏使用真实轻量 Demo 的 4,073 点 / 210 期口径，或明确标注“全量项目数据”和“网页演示抽样数据”，禁止混写 407K、75.58 万、210、248 等不同口径。
- 收束首页案例、技术流程和平台介绍，避免继续承担公司官网全部职能。

### Phase 2：Map 信息架构与 AnalysisContext

- 新建一个统一 `AnalysisContext`，至少包含：datasetId、datasetName、timeRange、filters、activeColorMode、selectedPointId、selectedRegion、selectedRegionStats 和地图视图。
- 左侧一级功能只保留“数据 / 图层 / 筛选”；右侧只保留“点位分析 / 区域分析 / AI 解读”。
- 顶部显示当前数据集、时间范围、筛选后点数和导出。
- 保留底部时间轴，但避免每个工具都成为顶部并列主操作。
- 将形变量 / 形变模式做成显式主切换，建立全站唯一模式顺序和颜色表。

### Phase 3：点位智能分析

- 在现有详情上增加“当前状态、长期速率、近一年速率、模式来源/置信度”。
- 只有 CSV/模型真实提供置信度时才显示数值，否则显示“未提供/暂无识别结果”。
- 曲线补充日期/形变量 Hover、时间缩放和恢复全时段。
- 智能解释只基于程序计算值和已有模式字段生成；不判断工程安全、不输出灾害预警。

### Phase 4：异常发现 + 区域分析

- 增加一次点击的“发现异常”：优先筛选明显沉降、加速模式，并排除低质量点。
- 当前没有可靠空间聚类算法，只能陈述“异常监测点数量”，不能声称发现若干聚集区。
- 区域统计继续复用矩形框选，但需保存几何范围、时间范围、筛选和统计结果。
- 选区 0 点、筛选 0 点要提供清除筛选/调整阈值操作。

### Phase 5：AI 区域解读

- 当前没有 AI 接口，需要新增与分析上下文绑定的区域解读服务或透明 Demo 实现。
- LLM 输入只能是程序计算后的结构化区域统计，不传原始点集。
- 输出固定为“区域概况 / 主要发现 / 值得关注 / 建议下一步”，并提供“查看分析依据”。
- 分析依据缺失项必须显示“未提供”；所有结论限制在数据解释，不做安全判定。
- 必须实现 AI Loading、Error、超时和重试状态。

### Phase 6：Datasets / Upload

- 将“上传成功”拆成：上传 → 字段识别 → 数据检查 → 用户确认 → 进入分析。
- 增加重复坐标、无法解析时间字段和低相干比例；保留原始文件，不静默删除或覆盖。
- 用户选择“应用推荐筛选”或“保留全部数据”后才进入 Map。
- 大文件需要真正的断点续传和后端异步转换；当前分块仅解决归档，不代表 2 GB 可在线分析。

### Phase 7：Statistics 连续性

- 使用同一 `AnalysisContext`，从 Map 携带数据集、选区、筛选和时间范围。
- 删除固定 Dashboard 数字，只显示当前分析对象的真实统计。
- 返回 Map 时恢复选区、筛选、时间轴和地图范围。

### Phase 8：其他页面收束

- Platform 只回答四项能力：数据理解、时空分析、模式识别、AI 辅助解读。
- Showcase 统一为“业务问题 → 数据 → 产品分析流程 → 发现 → 产品价值”。
- Solutions 从一级导航弱化，只保留场景概述。
- About 使用真实项目边界、本人职责和迭代思路，避免泛化技术宣传。
- Login 简化为“登录后可保存私人数据集和分析记录”。

### Phase 9：埋点

- 新建统一 `trackEvent(name, payload)`；开发环境先输出结构化日志。
- 覆盖 V2 指定的 page_view、demo、upload、point、region、filter、AI、statistics 和 export 事件。
- 记录 Demo 开始时间和第一次有效点位/区域结果时间，用于 Time to First Insight。

## 8. 未完成 UI

1. `/statistics` 是展示型静态页面，不读取当前数据集或选区。
2. Map 没有 AI 解读区域、AI 状态、分析依据和下一步动作。
3. 点位曲线没有 Hover 提示、缩放和恢复全时段。
4. 点位卡没有近一年速率、模式来源和置信度边界说明。
5. “发现异常”没有独立的一键入口；现有速率阈值和低相干筛选需要用户理解参数。
6. `分析报告` 目前调用浏览器打印，不是结构化分析报告导出。
7. 密码找回接口明确返回 503，邮件服务尚未配置。
8. 数据集字段映射/质检主要发生在 Map 内，Dataset 页面没有完整的确认式流程。
9. 数据质检没有重复坐标统计，也没有“应用推荐筛选 / 保留全部数据”的用户确认。
10. 私有数据上传有进度，但没有可靠的断点续传恢复、后台解析进度和异步转换任务。
11. 没有统一事件埋点和产品指标。
12. 多个页面使用大型 Hero 和同尺寸卡片，仍有明显模板化痕迹。

## 9. 潜在风险

### 9.1 数据可信度

- 首页显示 407K、36 景等指标，公开 Demo 实际为 4,073 点、210 期；V2 文档又举例 75.58 万点、248 期。若不区分“全量项目”与“网页 Demo”，会影响作品可信度。
- Statistics 当前固定数字容易被误认为真实分析结果。
- 形变模式颜色当前由字符串哈希产生，尚无全站统一顺序和颜色，无法保证跨页面一致。

### 9.2 性能

- CSV 使用 `file.text()` 一次性读入，解析后为每个点保存全部时序数组；几百 MB 以上会显著占用浏览器内存。
- 私有数据打开时会下载并拼接全部分块，再在浏览器解析；“允许归档 2 GB”不等于“浏览器可以分析 2 GB”。
- 地图在时间轴、色带或阈值变化时会重新遍历并重建全部 Leaflet 圆点；每次点击最近点搜索和矩形选择也是 O(n)。4,073 点可用，几十万点会卡顿。
- 当前没有空间索引、视域查询、聚合瓦片或时间序列按需读取。

### 9.3 状态连续性

- Map 的状态全部位于 `MapWorkspace` 本地 state，刷新、跳转 Statistics 或返回时会丢失。
- `?dataset=` 只标识私有数据集，未携带选区、筛选、时间和地图范围。
- Map 组件较长且状态密集，后续继续追加功能容易产生选择模式、图层和右栏状态互相覆盖。

### 9.4 服务与安全

- 仓库并存 Vercel、Cloudflare/Sites、Netlify 三套历史痕迹，部署配置和文档容易误导。
- 认证接口尚无登录频率限制、验证码或邮件验证；忘记密码未完成。
- 私有数据接口做了同源检查，但认证入口尚未采用同样的来源限制和限流策略。
- 当前用户元数据和 CSV 均依赖对象存储，没有独立可查询数据库；用户和数据集规模增长后列表、搜索和审计能力有限。

### 9.5 测试

- 现有自动化测试只确认首页服务端渲染包含品牌文字。
- 缺少 V2 要求的空 CSV、缺经纬度、无时序字段、0 点选区、0 点筛选、网络错误、未登录私有数据、AI 失败/超时测试。
- 缺少 Map 点选、框选、时间轴、图层显隐和 AnalysisContext 回退的交互回归测试。

### 9.6 发布与元数据

- `app/layout.tsx` 的 metadataBase 仍指向旧站点地址，静态入口的标题和描述仍使用旧“WebGIS/时序监测平台”定位。
- 生产发布主要走 Vite 静态构建，而部分说明和测试仍围绕 Vinext/Sites；必须先确定唯一生产链路再清理。

## 10. Phase 1 前置决策

为了最小改动并保持现有站点稳定，Phase 1 建议采用以下默认决策：

1. 保留现有 React + TypeScript + Leaflet + Vite/Vercel 运行方式，不换栈。
2. Demo 继续使用当前 4,073 点、210 期公开 CSV，所有页面统一标注为“网页公开示例”。
3. 首页主动作：`体验示例数据` 进入 `/map`；`上传我的数据` 进入 `/map` 并打开导入向导，仍无需登录。
4. 不在 Phase 1 改 Map 内部功能，只调整首页叙事、Demo 入口和全站名称口径。
5. 每阶段控制在 12 个文件以内，不新增一级路由，不新增依赖。

## 11. Phase 0 边界检查

- 修改文件：0 个产品代码文件。
- 新增文件：`AUDIT_V2.md`。
- 新组件：0。
- 新一级路由：0。
- 新依赖：0。
- UI 改动：0。
- 是否违反 V2 边界：否。

